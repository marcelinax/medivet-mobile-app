import { StyleSheet } from 'react-native';

export const bottomTabStyles = StyleSheet.create({
  buttonsContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  button: {
    marginRight: 12,
  },
});
